apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quickbooks_positions.fullname" . }}-test-connection"
  labels:
{{ include "quickbooks_positions.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "quickbooks_positions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
